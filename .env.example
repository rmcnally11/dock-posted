# Optional. Defaults to data/runtime locally, or /tmp/dock-posted on Vercel.
# DATA_DIR=/tmp/dock-posted

# Optional. Classic or fine-grained PAT with Contents write on rmcnally11/dock-posted.
# Used by `gh` / git for the `github` remote. Do not commit the real value.
# GH_TOKEN=
